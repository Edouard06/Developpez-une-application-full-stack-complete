// This interface defines the structure for user-related operations such as updating or retrieving user information.

export interface UserRequest {
    email: string;
    username: string;
    id: number;
}